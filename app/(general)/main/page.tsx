import { Metadata } from "next";
import * as React from "react";

export const metadata: Metadata = {
  title: "Main",
  description: "Main",
};

export interface IPageProps {}

export default function MainPage(props: IPageProps) {
  return <div>nare</div>;
}
